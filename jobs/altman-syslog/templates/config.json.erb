{
  "http_host": "<%= p("http_host") %>",
  "http_port": <%= p("http_port") %>,
  "syslog_priority": "<%= p("syslog_priority") %>",
  "syslog_options": "<%= p("syslog_options") %>",
  "syslog_facility": "<%= p("syslog_facility") %>",
  "syslog_indent": "<%= p("syslog_indent") %>"
}
